# Test Fixtures

Place a `sample-contract.pdf` file here for the contract upload E2E tests.

You can use any small PDF file (< 1MB recommended for fast tests).

## Required fixtures

| File | Purpose |
|------|---------|
| `sample-contract.pdf` | Valid contract for upload tests |
| `invalid-file.txt` | Invalid file type for rejection tests |
